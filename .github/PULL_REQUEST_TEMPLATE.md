## Summary

## Testing

- [ ] `python -m pytest tests/ -v` passes
- [ ] `npm --prefix vscode-extension test` passes (if the extension changed)
- [ ] `npm --prefix vscode-extension run package` succeeds (if the extension changed)

## Checklist

- [ ] Focused change, one PR per feature or fix (see `CONTRIBUTING.md`)
- [ ] Docs updated if user-facing behavior changed
