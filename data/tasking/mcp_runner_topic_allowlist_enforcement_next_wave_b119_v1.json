{
  "next_wave_id": "mcp_runner_topic_allowlist_enforcement_next_wave_b119_v1",
  "parent_task": "CLAUDE_TASK_MCP_RUNNER_TOPIC_ALLOWLIST_ENFORCEMENT_B119_V1",
  "date": "2026-07-05",
  "status": "proposal",
  "notes": "PROPOSAL only, not enqueued. B119 implemented the deterministic runner/topic allowlist in core.py (RUNNER_TOPIC_ALLOWLIST + check_runner_topic_allowlist(), wired into run_taskctl()) and fully enforces it on auto_pickup(). mark_review()/mark_done()/export_jsonl() gained optional runner/topic kwargs but server.py (forbidden to touch this task) does not yet pass identity to them, so those three MCP tool surfaces are not yet enforced end-to-end. Two follow-ups: close that server-wiring gap, and (already proposed in B118's next-wave) measure neural routing within the now-enforced boundary.",
  "follow_up_tasks": [
    {
      "task_id": "CLAUDE_TASK_MCP_RUNNER_TOPIC_ALLOWLIST_SERVER_WIRING_B120_V1",
      "goal": "Thread a runner/topic identity into the geoai_task_mark_review / geoai_task_mark_done / geoai_task_export_jsonl MCP tool signatures in server.py so the B119 allowlist (core.check_runner_topic_allowlist, already accepting these as optional kwargs) is enforced end-to-end for all write-gated actions, not just auto_pickup. Requires server.py mutation (explicitly forbidden for B119).",
      "mode": "implementation_gated_core_patch",
      "runner": "claude_coding",
      "topic": "coding",
      "acceptance": [
        "geoai_task_mark_review/mark_done/export_jsonl tool signatures accept runner (and topic where meaningful)",
        "core.mark_review/mark_done/export_jsonl called with the supplied identity (no behavior change to core.py needed -- kwargs already exist)",
        "codex-only actions (done, export-jsonl) reject any non-codex runner end-to-end via the real MCP tool call",
        "existing B119 smoke + shell tests still pass unmodified",
        "python3 AITools/taskctl.py verify passes"
      ],
      "allowed_writes": [
        "tools/geoai-task-mcp/src/geoai_task_mcp/server.py",
        "tools/geoai-task-mcp/tests/test_mcp_runner_topic_allowlist_server_wiring_b120_v1.sh"
      ],
      "forbidden": [
        "core_py_semantic_change_to_allowlist_matrix",
        "launch_queue_contract_mutation",
        "write_gate_disable",
        "git_add_A",
        "mixed_task_commit"
      ],
      "estimated_effort": "1 session",
      "depends_on": [
        "CLAUDE_TASK_MCP_RUNNER_TOPIC_ALLOWLIST_ENFORCEMENT_B119_V1"
      ]
    },
    {
      "task_id": "CLAUDE_TASK_MCP_RUNNER_TOPIC_ALLOWLIST_NEURAL_DRYRUN_B120_V1",
      "goal": "Run isolated neural dryrun: measure whether the model can learn to select allowed runner/topic pairs from task context and abstain when evidence is insufficient. The B119 allowlist is a deterministic safety boundary; the neural controller learns to route within it, never around it.",
      "mode": "isolated_neural_dryrun_no_runtime",
      "runner": "claude_capability_eval",
      "topic": "capability_eval",
      "acceptance": [
        "per-class routing accuracy above baseline on the 11 allowed runner/topic pairs + codex wildcard",
        "abstain when task context is ambiguous (no forced guess)",
        "model never selects a denied action for a runner (e.g., never emits 'done' for a non-codex runner)",
        "no regex/keyword authority introduced",
        "dryrun uses read-only tools as evidence extractors only"
      ],
      "estimated_effort": "1 session",
      "depends_on": [
        "CLAUDE_TASK_MCP_RUNNER_TOPIC_ALLOWLIST_ENFORCEMENT_B119_V1"
      ]
    }
  ]
}
