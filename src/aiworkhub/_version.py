"""Canonical AIWorkHub release version.

This literal is the single source of truth. Python package metadata reads it
through setuptools' dynamic ``attr`` provider; extension metadata is a checked
projection maintained by ``scripts/release_metadata.py``.
"""

__version__ = "0.9.73"
