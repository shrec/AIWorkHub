#!/usr/bin/env python3
"""Repository-local executable entry point for the VS Code Codex mux."""
from pathlib import Path
import sys

SRC = Path(__file__).resolve().parents[1] / "src"
sys.path.insert(0, str(SRC))

from geoai_task_mcp.app_server_mux import main

raise SystemExit(main())
